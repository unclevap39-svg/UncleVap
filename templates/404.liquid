<div class="error-page">
  <h1 class="error-code">404</h1>
  <p class="error-msg">{{ 'general.404.title' | t | default: 'Page introuvable' }}</p>
  <a href="{{ routes.root_url }}" class="btn-hero-cta">Retour à l'accueil</a>
</div>

<style>
.error-page {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 60vh;
  gap: 24px;
  padding: 40px 16px;
  text-align: center;
}
.error-code {
  font-family: 'Bebas Neue', sans-serif;
  font-size: clamp(80px, 20vw, 160px);
  color: var(--green);
  line-height: 1;
  text-shadow: 0 0 40px rgba(46,168,46,0.4);
}
.error-msg {
  font-family: 'Oswald', sans-serif;
  font-size: clamp(18px, 4vw, 28px);
  color: var(--text-muted);
}
</style>
